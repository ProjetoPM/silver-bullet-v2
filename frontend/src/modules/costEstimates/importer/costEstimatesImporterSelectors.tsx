import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const costEstimatesImporterSelectors = importerSelectors(
  'costEstimates.importer',
);

export default costEstimatesImporterSelectors;
